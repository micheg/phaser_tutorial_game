import { IMG } from '../cfg/assets';
import Phaser from 'phaser';

export default class BGMusic extends Phaser.Scene
{
    constructor ()
    {
        super({ key: 'bgm-scene', active: true });
    }

    create ()
    {
    }
};